{
  "name": "lyon-gtfs-etl",
  "version": "1.0.0",
  "private": true,
  "description": "ETL GTFS Lyon -> fichiers statiques par arret",
  "scripts": {
    "test": "node --test",
    "build": "node --max-old-space-size=6144 src/build.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "csv-parse": "^5.6.0"
  }
}
